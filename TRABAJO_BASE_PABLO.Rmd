---
title: "Estudio sobre los Cristales"
author: "Mario Álvarez Martínez, Jorge Aranda Honrubia, 
      Pablo de Puelles Carceles, Julia Ruiz Picó,  Cristina Tarraga Herrero (Grupo A1-10)"
date: "`r Sys.Date()`"
output: pdf_document
---


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, cache = FALSE, include = TRUE, warning = FALSE, message = FALSE)

# Función para cargar o instalar paquetes si faltan
cargar_paquete <- function(paquete) {
  if (!require(paquete, character.only = TRUE)) {
    install.packages(paquete, repos = "[https://cran.rstudio.com/](https://cran.rstudio.com/)", lib = Sys.getenv("R_LIBS_USER"))
    library(paquete, character.only = TRUE)
  }
}
install.packages("readr")
install.packages("tinytex")
cargar_paquete("readr")
cargar_paquete("tinytex")
```
# Exploración y preparación de la BBDD  [3 páginas máximo]

```{r}

data <- read_csv("10A_cristales.csv")
View(data)
```


## Descripción y naturaleza de las variables

<!-- Incluir tabla con las variables, su descripción y el tipo de variable (numérica, categórica nominal, categórica ordinal, identificador, fecha, etc.) -->

<!-- Indicar el número de observaciones iniciales en la BBDD. -->


```{r}
descData = data.frame("variable" = colnames(data),
                      "tipo" = c(rep("numerical", 11), 
                                 rep("categorical", 2), 
                                 rep("numerical", ncol(data) - 13)))
rownames(descData) = descData$variable
descData
```


## Variables constantes o casi constantes

Comprobaremos ahora si existe alguna variable que no varía (o que varía muy poco) y la eliminaremos también de la base de datos, ya que no se tratará de una variable aleatoria y no tiene sentido incluirla en el análisis estadístico. Esto se comprueba de forma diferente para las variables numéricas y para las variables categóricas.

En el caso de las variables numéricas, podemos calcular su desviación típica. Como la desviación típica depende de las unidades de medida y de la magnitud de la variable, es más aconsejable utilizar el coeficiente de variación.

Para las variables categóricas, obtendremos una tabla de frecuencias para comprobar si toman distintos valores.


```{r}
## Numéricas
# Filtramos para asegurar que solo usamos columnas que R reconoce como números
vars_num <- descData$variable[descData$tipo == "numerical"]
data_num <- data[, vars_num]
# Forzamos que todo sea numérico por si acaso
data_num <- as.data.frame(lapply(data_num, as.numeric))

summary(data_num)

# Variabilidad (desviación típica)
mySD = apply(data_num, 2, sd, na.rm = TRUE)

# Coeficiente de variación
myMU = colMeans(data_num, na.rm = TRUE)
myCV = mySD / myMU
sort(myCV)

## Categóricas
vars_cat <- descData$variable[descData$tipo == "categorical"]
apply(data[, vars_cat], 2, table, useNA = "ifany")

apply(data[, vars_cat], 2,
      function (x) round(100*table(x)/sum(table(x)), 2))
```



# Reglas de asociación  [OPCIONAL. 2 páginas máximo]


```{r}
library(arules)
library(arulesViz)
library(readr)
```
Para poder aplicar las reglas de asociación a nuestros datos, es esencial la transformación del tipo de dato. Para hacerlo correctamente, es necesario hacer primero una función de discretización que permita convertir todas las variables numéricas en categóricas, y tras esto, convertir el tipo de dato en transactions para poder usar la librería arules y aplicar el algoritmo apriori.
Para facilitar la transformación, se vuelve a tomar la basde de datos inicial.
```{r}
cristales <- read_csv("10A_cristales.csv")
cristales <- as.data.frame(cristales)

#Función de discretización utilizada, marcando presencia y cantidad de cada elemento y niveles de dureza y densidad
discretizar_con_ausencia <- function(x, umbral_cero = 0.001) {
  es_cero <- abs(x) < umbral_cero
  if (all(es_cero)) return(factor(rep("ausente", length(x)), levels = "ausente"))
  
  x_pos <- x[!es_cero]
  n_unique <- length(unique(x_pos))
  
  if (n_unique == 1) { labels <- "presente"; brks <- 1 }
  else if (n_unique == 2) { labels <- c("bajo", "alto"); brks <- 2 }
  else { labels <- c("bajo", "medio", "alto"); brks <- 3 }
  
  x_pos_disc <- discretize(x_pos, method = "frequency", breaks = brks, labels = labels)
  
  resultado <- vector("character", length(x))
  resultado[es_cero] <- "ausente"
  resultado[!es_cero] <- as.character(x_pos_disc)
  
  niveles_final <- intersect(c("ausente", "bajo", "medio", "alto", "presente"), unique(resultado))
  factor(resultado, levels = niveles_final)
}

#Transformación de numérico a categórico
columnas_num <- c("RI", "Na", "Mg", "Al", "Si", "K", "Ca", "Ba", "Fe", "hardness", "density", "thermal_exp")

for(col in columnas_num) {
  if(col %in% names(cristales)) {
    cristales[[col]] <- discretizar_con_ausencia(cristales[[col]])
  }
}

#Transformación de variables categóricas
cristales$color <- as.factor(cristales$color)
cristales$tranparency <- as.factor(cristales$tranparency)
cristales$Type <- as.factor(cristales$Type) 

# Se elimina del análisis la variable de los identificadores
cristales$id <- NULL

# Se convierte a transacciones
trans <- as(cristales, "transactions")


```
Por el interés para el análisis, las reglas seleccionadas se centran en las variables que aparecen con más frecuencia para los tipos de cristal como consecuentes, mostrando así las relaciones entre el uso de cada tipo dematerial y su composición o sus cualidades. Primero se hacen todas las reglas posibles que tengan como consecuente el tipo (y solamente el tipo), ordenándolas en función del lift que tengan. Además, se tiene en cuenta que para el tipo 3 solo hay 13 individuos, por lo que para que haya reglas que sean representativas de los cristales más comunes y también reglas que tengan en cuenta la menor cantidad de cristales de este tipo, se harán dos grupos de reglas con soportes diferentes.
```{r}
reglas_tipos_comunes <- apriori(trans, parameter = list(supp = 0.05, conf = 0.8, minlen = 2),
                       appearance = list(rhs = c("Type=1", "Type=2", "Type=4", "Type=5"), default = "lhs"))
reglas_tipo_3 <- apriori(trans, parameter = list(supp = 0.02, conf = 0.8, minlen = 2),
                       appearance = list(rhs = c("Type=3"), default = "lhs"))
reglas_combinadas <- c(reglas_tipos_comunes, reglas_tipo_3)
reglas_ordenadas <- sort(reglas_combinadas, by = "lift")
summary(reglas_ordenadas)
```
Se obtienen 514 reglas, por lo que es necesario pasar a un filtrado en el que se elimine la redundancia.

```{r}
reglas_final <- reglas_ordenadas[!is.redundant(reglas_ordenadas)]
inspect(head(reglas_final, 10))
```
En este primer análisis, se puede ver cómo todas las reglas de mayor lift tinen como consecuente el tipo 3, con un lift de 14, pero con soportes más bajos. Esto se deberá a que los cristales de tipo 3 son una minoría en la base de datos, pero todos los cristales de este tipo tienen composiciones idénticas, llegando así a este lift máximo. Para poder ver la influencia en todos los tipos, se buscan y analizan las primeras reglas de cada uno de los tipos, es decir, aquella regla con un mayor lift para cada utilidad de los cristales.
```{r}
mejor_tipo1 <- head(subset(reglas_final, rhs %in% "Type=1"), 1)
mejor_tipo2 <- head(subset(reglas_final, rhs %in% "Type=2"), 1)
mejor_tipo3 <- head(subset(reglas_final, rhs %in% "Type=3"), 1)
mejor_tipo4 <- head(subset(reglas_final, rhs %in% "Type=4"), 1)
mejor_tipo5 <- head(subset(reglas_final, rhs %in% "Type=5"), 1)
inspect(c(mejor_tipo1, mejor_tipo2, mejor_tipo3, mejor_tipo4, mejor_tipo5))
```
Tras este anáslisis, se puede ver reflejado cómo de común es cada cristal: tipo 1 y tipo2, que son los más comunes, presentan reglas con mayor count y menor lift que los otros 3 tipos, con el caso excepcional del tipo 3 en el que la regla obtenida tiene un lift muy superior pero con menos soporte que el resto.
El tipo 1 se caracterizaría por Un índice de refracción medio (RI) y un nivel de Mg medio. El tipo2, por un RI bajo, nivel medio de Mg, nivel alto de K y ausencia de Fe. El tipo de tres tendría un nivel medio de Ca, una dureza baja, sería transparente y una exposición térmica baja. El tipo 4 tendría un Ca alto, una ausencia de Ba y un color opaco. Finalmente, el tipo 5 tendría un nivel alto de Al y un nivel medio de Ba. Estas diferencias reflejan cómo, según la composición de un cristal, se obtienen distintas propiedades, y cómo, en función de estas propiedades se le dará un uso u otro a cada cristal.