---
title: "Estudio sobre los Cristales"
author: "Mario Álvarez Martínez, Jorge Aranda Honrubia, 
      Pablo de Puelles Carceles, Julia Ruiz Picó,  Cristina Tarraga Herrero (Grupo A1-10)"
date: "`r Sys.Date()`"
output: pdf_document
---


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, cache = FALSE, include = TRUE, warning = FALSE, message = FALSE)

# Función para cargar o instalar paquetes si faltan
cargar_paquete <- function(paquete) {
  if (!require(paquete, character.only = TRUE)) {
    install.packages(paquete, repos = "[https://cran.rstudio.com/](https://cran.rstudio.com/)", lib = Sys.getenv("R_LIBS_USER"))
    library(paquete, character.only = TRUE)
  }
}

cargar_paquete("readr")
cargar_paquete("tinytex")
```
# Exploración y preparación de la BBDD  [3 páginas máximo]

```{r}

data <- read_csv("10A_cristales.csv")
View(data)
```


## Descripción y naturaleza de las variables

<!-- Incluir tabla con las variables, su descripción y el tipo de variable (numérica, categórica nominal, categórica ordinal, identificador, fecha, etc.) -->

<!-- Indicar el número de observaciones iniciales en la BBDD. -->


```{r}
descData = data.frame("variable" = colnames(data),
                      "tipo" = c(rep("numerical", 11), 
                                 rep("categorical", 2), 
                                 rep("numerical", ncol(data) - 13)))
rownames(descData) = descData$variable
descData
```


## Variables constantes o casi constantes

Comprobaremos ahora si existe alguna variable que no varía (o que varía muy poco) y la eliminaremos también de la base de datos, ya que no se tratará de una variable aleatoria y no tiene sentido incluirla en el análisis estadístico. Esto se comprueba de forma diferente para las variables numéricas y para las variables categóricas.

En el caso de las variables numéricas, podemos calcular su desviación típica. Como la desviación típica depende de las unidades de medida y de la magnitud de la variable, es más aconsejable utilizar el coeficiente de variación.

Para las variables categóricas, obtendremos una tabla de frecuencias para comprobar si toman distintos valores.


```{r}
## Numéricas
# Filtramos para asegurar que solo usamos columnas que R reconoce como números
vars_num <- descData$variable[descData$tipo == "numerical"]
data_num <- data[, vars_num]
# Forzamos que todo sea numérico por si acaso
data_num <- as.data.frame(lapply(data_num, as.numeric))

summary(data_num)

# Variabilidad (desviación típica)
mySD = apply(data_num, 2, sd, na.rm = TRUE)

# Coeficiente de variación
myMU = colMeans(data_num, na.rm = TRUE)
myCV = mySD / myMU
sort(myCV)

## Categóricas
vars_cat <- descData$variable[descData$tipo == "categorical"]
apply(data[, vars_cat], 2, table, useNA = "ifany")

apply(data[, vars_cat], 2,
      function (x) round(100*table(x)/sum(table(x)), 2))
```

Por ahora se decide no eliminar ninguna variable. 



## Análisis descriptivo


# Valores inconsistentes o anómalos

Analizaremos ahora si alguna variable tiene valores inconsistentes, es decir, valores que no son posibles dentro del rango de definición de la variable y que por tanto se deben a un error. En ese caso, estos valores se sustituirán por NA convirtiéndose en valores faltantes o perdidos. Los valores inconsistentes se pueden observar tanto en variables numéricas como categóricas.

También estudiaremos la existencia de posibles valores anómalos en las variables numéricas. A veces, estos valores anómalos pueden deberse a un error (serían valores inconsistentes y se sustituirían por NA).Pero otras veces, los valores anómalos son valores posibles pero muy atípicos o extremos. En este último caso, debemos tomar la decisión de mantener la observación anómala en el modelo o eliminarla (no se reemplaza el valor anómalo por NA, sino que se elimina el registro completo).

En esta práctica, estudiaremos los valores anómalos desde el punto de vista univariante (se analiza cada variable por separado). Veremos en la Práctica 1 cómo utilizar el Análisis de Componentes Principales (PCA) para estudiar los valores anómalos desde el punto de vista multivariante.

El resumen descriptivo de las variables categóricas (tablas de frecuencias anteriores) no mostró ningún valor inconsistente.

A continuación se estudian gráficamente las variables numéricas. En primer lugar se realiza un gráfico de cajas y bigotes con todas las variables numéricas. Sin embargo, dado que cada una tiene rangos diferentes, se representan los datos en escala logarítmica para mitigar un poco dichas diferencias y poder visualizarlas mejor. Después, realizaremos gráficos individuales para algunas de las variables que más llaman nuestra atención en este gráfico de cajas y bigotes múltiple.



<!-- Incluir resultados que muestren posibles anomalías detectadas e indicar cómo se han corregido. -->

<!-- Si se elimina alguna observación o variable, indicarlo claramente y justificarlo. -->
# Transformación y recodificación de variables

Antes de avanzar, es necesario revisar si nuestras variables necesitan algún ajuste para que el análisis sea más fluido. En este apartado evaluaremos si la composición química y las características cualitativas del cristal, como el color, están en el formato óptimo. Veremos si es mejor reagrupar algunas categorías pequeñas o crear variables binarias que nos ayuden a simplificar la información. No buscamos cambiar los datos por cambiar, sino adaptarlos para que la base de datos esté bien alineada con el foco de nuestro estudio y responda mejor cuando empecemos a clasificar los tipos de cristales.

## Transformar variables categóricas a variables 0-1.

Comenzaremos con la transformación de las variables categóricas para asegurar su compatibilidad con los modelos analíticos. Convertiremos las categorías de los tipos de colores o transparencia de los cristales en variables numéricas de 0 y 1. Esta recodificación es fundamental porque transformare la información cualitativa en datos cuantitativos que los algoritmos de clasificación o PCA puedan procesar eficientemente, permitiéndonos integrar todas las propiedades del cristal en un único conjunto de datos numérico.

```{r}
matriz_dummies <- model.matrix(~ 0 + color + tranparency, data = data)

head(matriz_dummies)
```

Esta matriz la añadiremos como columnas nuevas a la base de datos para cada tipo de color y nivel de transparencia. En cada fila (cristal), se marcará con un '1' la columna que corresponda a su característica real, y con un '0' aquellas que no apliquen, permitiendo así que el modelo identifique sus propiedades cualitativas mediante valores numéricos.

```{r}
data_dummies <- as.data.frame(matriz_dummies)
data_final <- cbind(data[, !(names(data) %in% c("color", "tranparency", "id"))], data_dummies)
head(data_final)
```
Tras esta transformación, disponemos de una base de datos completamente numérica y mejor estructurada, garantizando la compatibilidad necesaria para aplicar con precisión técnicas avanzadas como PCA y clustering en los próximos análisis.

## Reagrupar categorías poco frecuentes

Tras analizar la distribución de la variable color, se ha observado que la mayoría de las observaciones se concentran en dos categorías principales, existiendo una tercera clase denominada 'Others' que agrupa eficientemente las tonalidades minoritarias. Dado que esta estructura ya garantiza que ninguna categoría tenga una frecuencia insignificante que pudiera desestabilizar los modelos matemáticos posteriores, se ha decidido mantener la variable tal como está, procediendo directamente a su transformación en variables dummy sin necesidad de realizar reagrupaciones adicionales.

## Transformaciones para conseguir normalidad o simetría (Autoescalado)

Con la base de datos ya convertida a un formato puramente numérico, el siguiente paso fundamental es la normalización (o estandarización) de las variables. Dado que características como el índice de refracción (RI) y el contenido de sodio (Na) tienen escalas y unidades de medida muy distintas, es crucial ajustar todas las variables para que tengan una media de cero y una desviación estándar de uno. Esto evita que las variables con valores numéricos más altos dominen artificialmente los cálculos de distancia, asegurando que técnicas como el PCA y el clustering ponderen correctamente la influencia de todas las propiedades del cristal, independientemente de su magnitud original.

Para determinar el método de normalización más adecuado, realizaremos una comparación visual entre la distribución original de las variables numéricas y su comportamiento tras aplicar una transformación logarítmica. Mediante gráficos de densidad, evaluaremos qué enfoque corrige mejor la asimetría de los datos, asegurando que la estructura resultante sea óptima para los análisis multivariantes posteriores.
```{r}
plot(density(data$Na), main = "Densidad Na (Original)", 
     col = "blue3", lwd = 2, xlab = "Contenido de Sodio")


data$Na_log <- log2(data$Na)
plot(density(data$Na_log), main = "Densidad Na (Transformada log2)", 
     col = "red3", lwd = 2, xlab = "Log2(Na)")

columnas_numericas <- c("RI", "Na", "Mg", "Al", "Si", "K", "Ca", "Ba", "Fe")
data_normalizada <- data
data_normalizada[columnas_numericas] <- scale(data[columnas_numericas])

colMeans(data_normalizada[columnas_numericas])
apply(data_normalizada[columnas_numericas], 2, sd)
```
La necesidad de estandarizar las variables mediante un Z-score (ajustando media a cero y desviación estándar a uno) radica en la disparidad de escalas y unidades de medida entre los componentes químicos del cristal. Este paso es crucial para evitar que variables con valores absolutos elevados dominen el análisis, garantizando que el PCA y el Clustering detecten patrones basados en la verdadera relevancia estadística de cada característica, no solo en su magnitud.

```{r}
# 1. Definir las columnas numéricas que se van a estandarizar

columnas_a_escalar <- c("RI", "Na", "Mg", "Al", "Si", "K", "Ca", "Ba", "Fe", 
                        "Type", "hardness", "density","thermal_exp",
                        "colorCL", "colorOT", "colorWH",
                        "tranparencyTL","tranparencyTP") 

# 2. Aplicar el escalado (Z-score)
data_final_escalada <- data_final
data_final_escalada[columnas_a_escalar] <- scale(data_final[columnas_a_escalar])

# 3. Comprobar resultados 

print("Medias (deben ser casi 0):")
print(round(colMeans(data_final_escalada[columnas_a_escalar]), 5))
print("Desviaciones típicas (deben ser 1):")
print(round(apply(data_final_escalada[columnas_a_escalar], 2, sd), 5))
```
Tras finalizar la estandarización de las variables, se ha verificado correctamente centrando y escaladno nuestras variables numeriacas obteniendo una media de cero y una desviación de uno. Este resultado confirma que la base de datos está perfectamente equilibrada y optimizada para las técnicas de PCA y clustering.

# Análisis de Componentes Principales  [5 páginas máximo]


## Tratamiento de los datos

<!-- Selección de variables a incluir en el modelo? Centrado y escalado? Transformación de variables? -->


## Selección del número de componentes principales

Voy a comenzar con la selección de componenetes principales. 





## Validación del modelo

<!-- Detección de valores anómalos y decisiones tomadas a partir de los mismos. -->


## Interpretación del modelo

<!-- Interpretación del gráfico de scores y de loadings o de variables. -->


## Conclusiones




# Reglas de asociación  [OPCIONAL. 2 páginas máximo]



# Análisis Factorial de Correspondencias  [OPCIONAL. 2 páginas máximo]

